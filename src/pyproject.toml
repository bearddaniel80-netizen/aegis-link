[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aql_link"
version = "0.0.1"
description = "AQL Link Framework"
authors = [{ name = "Daniel Beard" }]
requires-python = ">=3.10"
dependencies = [ ]